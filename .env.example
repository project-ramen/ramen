# Docker Compose에서 사용. 복사 후 .env 로 저장하고 값을 채우세요.
#
# PostgreSQL (compose의 postgres 서비스). 프로덕션에서는 반드시 강한 비밀번호로 변경.
# POSTGRES_PASSWORD=your-db-password
# 호스트에 이미 5432가 쓰이면 예: POSTGRES_PORT=5433
#
# 관리자 API(포스트·리비전 등) 인증용. 앱 블로그 연결 시 같은 비밀번호 입력.
# RAMEN_ADMIN_PASSWORD=your-secret-password
